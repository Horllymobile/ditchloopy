package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// DitchLoopy Sophisticated Dark Theme Colors
val SophisticatedDarkBackground = Color(0xFF0A0C0A)  // Deep forest black background
val SophisticatedLightBackground = Color(0xFFF2F5F2) // Light elegant gray-green background

val SageAccentPrimary = Color(0xFF243324)        // Deep moss container color
val SageAccentPrimaryLight = Color(0xFF425647)   // Soft organic forest primary for light mode

val SageBright = Color(0xFFA8D5BA)               // Bright pastel sage green highlight
val MutedSageText = Color(0xFFA8B5A8)             // Muted sage gray/green for subtexts

val SophisticatedBorder = Color(0xFF2A352A)      // Deep moss green outline border
val SophisticatedBorderLight = Color(0xFFD4DFD4) // Soft sage border light

val TerracottaAccent = Color(0xFFC56D48)         // Warm terracotta highlight
val OchreAccent = Color(0xFFD4C5B0)              // Soft wheat champagne highlight

val CardSurfaceDark = Color(0xFF162016)          // Sophisticated dark gradient primary card surface
val CardSurfaceDarkSecondary = Color(0xFF0F140F) // Sophisticated dark gradient end card surface
val CardSurfaceLight = Color(0xFFFFFFFF)         // White clean surface

val TextPrimaryDark = Color(0xFFE8EDE8)          // Off-white clean text
val TextPrimaryLight = Color(0xFF0A0C0A)         // Deep forest black text

val TextSecondaryDark = Color(0xFFA8B5A8)        // Muted linen gray
val TextSecondaryLight = Color(0xFF5A665A)       // Soft sage text muted
